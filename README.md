# pwncollege
- This repository will contain my pwncollege writeups


# pwn_lab

- A docker container for setting up an reverse engineering environment for testing vulnerable binaries specifically designed to test **pwncollege** binaries.

**NOTE** : This docker container is for **pwncollege** only so its not a SECURE ISOLATED ENVIRONMENT completely. Best usecase is to run this inside a VM itself so better security.